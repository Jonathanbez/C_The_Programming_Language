#include <stdio.h>

//Format Specifiers for tell the compiler about the type of data
//in operations of input and output

int main()
{
    int x = 10, y = 30;
    char c = "X"
    printf("%d%d%c\n", x,y,c);
}